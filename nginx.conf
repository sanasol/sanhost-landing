server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Security headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Cache static assets
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|webp|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # robots.txt and sitemap.xml
    location = /robots.txt {
        expires 1d;
    }
    location = /sitemap.xml {
        expires 1h;
    }

    # Proxy Forgejo release API (avoids CORS issues)
    location = /api/latest-release {
        proxy_pass https://git.sanhost.net/api/v1/repos/sanasol/f2p-evo/releases/latest;
        proxy_set_header Accept "application/json";
        proxy_ssl_server_name on;
        add_header Cache-Control "public, max-age=600";
        add_header Access-Control-Allow-Origin "*";
    }

    # Language-prefixed routes
    location ~ ^/(ru|uk|de|fr|es|pt|it)/$ {
        try_files /$1/index.html =404;
    }

    # Language-prefixed routes without trailing slash — redirect to add slash
    location ~ ^/(ru|uk|de|fr|es|pt|it)$ {
        return 301 /$1/;
    }

    # Default (English) root
    location / {
        try_files $uri $uri/ /index.html;
    }
}
